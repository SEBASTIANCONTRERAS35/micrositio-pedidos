apiVersion: v1
kind: Service
metadata:
  name: worker
  namespace: micrositio
  labels:
    app: worker
spec:
  type: ClusterIP
  selector:
    app: worker
  ports:
    - name: metrics
      port: 3001
      targetPort: metrics
      protocol: TCP
