apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: micrositio
  labels:
    app: api
spec:
  type: ClusterIP
  internalTrafficPolicy: Local
  selector:
    app: api
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
